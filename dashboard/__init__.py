"""HireIQ V1 dashboard package.

A read-only visualization layer for the HireIQ candidate ranking pipeline.
The dashboard never computes rankings or mutates outputs; it only reads and
presents the artifacts produced by the existing pipeline.
"""

__version__ = "1.0.0"
